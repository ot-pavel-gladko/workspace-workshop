Publish the Artisyn catalog to cloud (dry run by default).

Run:
```bash
python3 workspace.py publish --agent claude
```

To actually publish:
```bash
python3 workspace.py publish --confirm --agent claude
```
