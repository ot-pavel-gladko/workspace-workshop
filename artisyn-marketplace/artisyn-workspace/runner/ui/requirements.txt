# UI sidecar dependencies — pinned per ADR-0004 and ADR-0005 version governance.
#
# Chat surface: Chainlit (Apache-2.0) — version-pinned dependency, not vendored.
# Activity dashboard: Langfuse (MIT) — self-hosted trace replay + KPI surfacing.
#
# Bump versions intentionally (ADR-0005: breaking changes require ADR or governed bump).
chainlit==2.5.5
# Langfuse Python SDK pinned to the 2.x line to match BOTH the self-hosted v2
# server (docker-compose) AND app.py's API usage (client.trace()/.generation()/
# .flush()), which the 3.x SDK removed. SDK major must track server major.
langfuse==2.60.10
# Transitive-completion pin: chainlit -> asyncer -> anyio import `sniffio` at
# runtime, but anyio==4.13.0's wheel metadata omits sniffio from Requires-Dist,
# so the resolver doesn't pull it. Pin it explicitly so the venv is complete.
sniffio==1.3.1
